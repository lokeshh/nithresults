%h1 Student Results

%table#students
  %thead
    %tr
      %th= link_to 'Roll No.', root_path(:sort_by => :rollno)
      %th= link_to 'Name', root_path(:sort_by => :name)
      %th= link_to 'CGPI', root_path(:sort_by => :cgpi)
      
  %tbody
    - @students.each do |s|
      %tr
        %td= s.rollno
        %td= s.name
        %td= s.cgpi