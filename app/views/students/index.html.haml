%h1 Students Results

%table#students
  %thead
    %tr
      %th Roll No.
      %th Name
      %th CGPI
      
  %tbody
    - @students.each do |s|
      %tr
        %td= s.rollno
        %td= s.name
        %td= s.cgpi